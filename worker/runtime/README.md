# Worker Runtime Area

Use this area for worker bootstrap, scheduler entrypoints, queues, job contracts, and execution wiring.

Keeping runtime separate from business logic makes later monitoring and retry handling easier.
